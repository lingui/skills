{
  "skill_name": "enhanced-message-context",
  "evals": [
    {
      "id": 1,
      "eval_name": "ambiguous-strings-comments",
      "prompt": "Translators keep mistranslating strings from this app. Add translator context to the messages in src/components so every ambiguous string is clear. Code changes only.",
      "expected_output": "Ambiguous messages (Back, Post, Archive, column headers, the selected-count message) carry comment fields describing location and purpose; context props are not abused for organization.",
      "files": ["evals/enhanced-message-context/files/publishing-dashboard"],
      "expectations": [
        "The Back button Trans gets a comment describing where it appears and what it does (a navigation action), not a definition of the word",
        "The Post button comment disambiguates it as a verb (publish action), consistent with the app being a blog publishing platform per package.json's description",
        "The Archive t-call gets a comment — converted to the t({ comment, message }) object form or an equivalent that attaches a comment",
        "Each table column header label (Name, Status, Created) gets a comment saying what the column shows",
        "The '{selectedCount} selected' message gets a comment clarifying what is selected and what the placeholder value is",
        "comment is used throughout — the context prop is not introduced, and no namespace-like values (e.g. toolbar.back) appear anywhere",
        "Comments describe location and referent rather than defining word meanings, and are concise (roughly under 120 characters each)"
      ]
    },
    {
      "id": 2,
      "eval_name": "comment-vs-context-collision",
      "prompt": "Translators report that 'right' needs two different translations in this app: in the editor it means the alignment direction, in the quiz it means a correct answer — but the catalog only has one entry so one of them is always wrong. Fix that, and add whatever translator context the rest of the messages in src/ need. Code changes only.",
      "expected_output": "The two 'right' occurrences are split into separate catalog entries via distinct context values; identical-meaning Save buttons stay one entry (comments only); the explanation distinguishes comment from context.",
      "files": ["evals/enhanced-message-context/files/course-authoring"],
      "expectations": [
        "Both <Trans>right</Trans> occurrences get a context prop with distinct values (one for text alignment, one for a correct answer), producing two catalog entries",
        "The two Save buttons do NOT get different context values — identical meaning stays one catalog entry; adding comments to them is acceptable",
        "The alignment left/center/right button group is treated consistently (e.g. 'left' gets the same alignment context or equivalent consistent handling as 'right')",
        "'wrong' in the quiz gets treatment consistent with the graded-answer 'right' (context or comment referencing the quiz result)",
        "The explanation distinguishes comment (translator hint, same catalog entry) from context (changes message identity, separate entries)"
      ]
    },
    {
      "id": 3,
      "eval_name": "directive-scope-and-tagged-templates",
      "prompt": "We're sending this app for translation next week. Go through src/components and make sure every message that needs translator context has it, and that the context each message gets is actually about that message. Code changes only.",
      "expected_output": "The unclosed lingui-set directive in ConversationHeader.tsx is scoped to the action buttons it describes, the status labels get their own context instead of inheriting it, and the tagged templates in EmptyState.tsx end up with comments while the self-explanatory sentence is left alone.",
      "files": ["evals/enhanced-message-context/files/support-inbox"],
      "expectations": [
        "The existing `lingui-set comment=\"Action buttons in the header of a conversation\"` in ConversationHeader.tsx is closed with `lingui-reset` (or replaced by per-message comments) so it stops applying past the action array",
        "The Resolved and Open status labels do not end up carrying the action-buttons comment; they get their own context describing them as the state of a conversation",
        "The Close action label is disambiguated as the verb (closing or resolving a conversation), not the adjective or the proximity sense — either by its own comment or by a directive comment specific enough to carry it",
        "The `Empty inbox` alt text and the `New` button label get comments, via the `t({ comment, message })` object form or a `lingui-set` block covering them — a tagged template left bare is a miss",
        "`New` is disambiguated as starting a new conversation rather than read as an adjective",
        "Any `lingui-set` directive introduced by the agent is closed with `lingui-reset` rather than left open to the end of the file",
        "The \"You're all caught up...\" sentence is left uncommented or given at most a brief location note — it carries its own context and is not a must-comment message",
        "`context` is not introduced for any of these messages; none of them is a case of the same text needing separate catalog entries"
      ]
    }
  ]
}
